import { TransportFilterPipe } from './transport-filter.pipe';

describe('TransportFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new TransportFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
